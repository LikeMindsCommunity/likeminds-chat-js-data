import { API } from '../shared/api.constant';
import { Base } from '../base';
import { InIt } from './types';

export class Posts extends Base {
    // initSDK(sdk: InIt): Promise<any> {
    //   return this.invoke(`${API.SDK_RESOURCE}`, {
    //     method: "POST",
    //     body: JSON.stringify(sdk),
    //   });
    // }
}
